# pil 作業ルール

自家用操縦士（回転翼）学科試験の勉強用ツリー。まとめノート・過去問・
印刷用の組版スクリプトが入っている。

このツリーの実体は `~/project/pil`。2026-09-20 に `~/pil` からここへ移したので
`~/pil` はもう無い。パスは `~/project/pil/…` で書く（移動時にスクリプトの
絶対パスは書き換え済み）。`~/project` から起動しているときも、pil 配下を触る前に
このファイルを読む。

## 編集したらコミットしてプッシュまでする

ファイルを直したら、**その場で `git commit` と `git push` まで済ませる。**
毎回確認を取らない。「変更しました」で止めない。

プッシュは非対話でも通る。リモートは SSH（`git@github-pil:yonemotoBB/pil.git`）で、
パスフレーズ無しの deploy key（`~/.ssh/id_ed25519_pil`、`~/.ssh/config` の
`Host github-pil` エイリアス、write 権限付き）を使うため認証で止まらない。

- HTTPS のままでは認証情報が無く非対話で失敗する。2026-07-16 に SSH へ切り替え済み
- `id_ed25519_bitflyer` / `id_ed25519_crypto_ops` は別リポジトリ用。pil では使わない
- 確認は `ssh -T git@github-pil`（"Hi yonemotoBB/pil!" が返る）

**r-18 側とは逆**（あちらは頼まれない限りプッシュしない）。混同しない。

## コミットメッセージ

日本語で書く。1行目は `範囲: 要約`。既存の履歴に合わせる。

```
過去問: 空中航法「計算盤不要問題集」を新規作成（全41問・重複排除）
印刷用: 法規を一条一ページ＋メモ欄に組んだ印刷版と生成スクリプトを追加
```

なぜそうしたかを本文に書く。特に、削除・修正は**何を根拠に誤りと判断したか**を残す
（過去問の正答修正は一度 revert している。根拠が曖昧なまま直さない）。

## 中身

| 場所 | 中身 |
|---|---|
| `PIL_*.md` | 科目ごとのまとめ（ATC / 法規 / 気象学 / 工学） |
| `chapters/` | 章ごとのノート |
| `過去問/` | 期別の過去問・例題集と、`build_all.py` などの組版スクリプト |
| `印刷用/` | 印刷用に組んだ成果物と生成スクリプト |

## コミットしないもの

`.gitignore` にあるもの（`*.pdf` `__pycache__/` `.vscode/`）は生成物。
`過去問/tmp/` のような中間物も入れない。

生成した PDF は再生成できる。消す前に生成スクリプトが揃っているかだけ確認する。
